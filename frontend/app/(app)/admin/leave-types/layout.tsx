export const metadata = {
  title: "Leave Type Setup",
  description: "Configure and manage leave types for your organization",
};

export default function LeaveTypesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
