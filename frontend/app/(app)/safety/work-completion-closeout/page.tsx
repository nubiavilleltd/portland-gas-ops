import SafetyWorkflowListPage from "@/components/safety/SafetyWorkflowListPage";

export default function WorkCompletionCloseOutPage() {
  return (
    <SafetyWorkflowListPage
      formKey="work_close_out"
      baseHref="/safety/work-completion-closeout"
    />
  );
}
